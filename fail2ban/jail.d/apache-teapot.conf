# Copy to /etc/fail2ban/jail.d/teapot.conf
# Adjust logpath if your Apache access log is elsewhere

[apache-teapot]
enabled = true
filter = apache-teapot
port = http,https
logpath = /var/log/apache2/access.log
maxretry = 1
